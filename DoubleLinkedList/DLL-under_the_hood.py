# Example of how a Doubly Linked List works under the hood using a dictionary
head = {
    "value": 7,  # Value of the node
    "next": None,  # Pointer to the next node (None since this is the only node)
    "prev": None  # Pointer to the previous node (None since this is the only node)
}